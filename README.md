An attempt to write a lexer and parser for rust, in rust, using formal
grammars and rust-based tools (lex and yacc like) that process them.

I.e. an attempt at a mostly-not-hand-written rust frontend.
